<h1>Demand Response Page</h1>

<div id="demand_form" style="text-align: right">
  <%= form_tag('/', :method => :put) do %>
  <table>
    <tr>
      <td>Enter the address of the Demand Response center:</td><td><%= text_field_tag :address %></td>
    </tr>
    <tr>
      <td>Enter the Demand Radius in Miles:</td><td><%= text_field_tag :radius %></td>
    </tr>
    <tr>
      <td>Enter the thermostat Set Point:</td><td><%= text_field_tag :setting %></td>
    </tr>
    <tr><td/><td><%= submit_tag 'Set Thermostats' %></td></tr>
  </table>
  <% end %>
</div>

<div id="response_locations" style="text-align: right">
  <%= render 'responses' %>
</div>
